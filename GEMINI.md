# CL8 Project Instructions

## Workflow Requirements
- **Global Updates:** Whenever code modifications are made to this project, you MUST automatically run the following commands to ensure the user's global `cl8` command reflects the latest changes:
  ```powershell
  npm run build
  npm install -g .
  ```
  Do not wait for the user to ask; execute these commands immediately after confirming code edits.